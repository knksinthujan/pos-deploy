// These are commented out in local development because of local .env.* files
// that pass configuration to the app.
// During deployment, this file should be replaced with one that contains
// the variables uncommented and with the actual configuration

// Target environment. For DevCloud we have special handling
// Possible values: "devcloud" | "production"
// window.REACT_APP_TARGET_ENV = 'devcloud';

// App API URL
// window.REACT_APP_API_URL = 'https://example.com/api';
